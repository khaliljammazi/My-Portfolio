// Tailwind config neutralized per user request.
// The original config was added by an assistant change and is intentionally removed.
// Keeping an empty export so build tools that import this file don't fail.
module.exports = {};
